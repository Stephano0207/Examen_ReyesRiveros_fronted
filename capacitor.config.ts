import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Examen_ReyesRiveros_fronted',
  webDir: 'www'
};

export default config;
